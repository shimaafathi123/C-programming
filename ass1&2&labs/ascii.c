#include <stdio.h>
#include <stdlib.h>

int main()
{
    char ascii='a';
    printf("Enter the character please: ");
    scanf("%c", &ascii);
    printf("The ascii of character is: %d", ascii);
    return 0;
}
